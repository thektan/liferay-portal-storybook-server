import React from "react";

import TestConfigurationButton from "src/main/resources/META-INF/resources/js/components/semantic_search_configuration/TestConfigurationButton";

export default {
	title: "SemanticSearch/TestConfigurationButton",
	component: TestConfigurationButton,
};

const Template = (args) => <TestConfigurationButton {...args} />;

export const Default = Template.bind({});
